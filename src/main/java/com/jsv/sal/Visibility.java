package com.jsv.sal;

/**
 * check if the object is visible on the page
 */
public interface Visibility {

    /**
     * check the objects visibility
     * @return Boolean true if the object is visible on the page.
     */
    boolean isVisible();
}
